import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  mainCont: { flex: 1, backgroundColor: "white", paddingHorizontal: 12 },
  msgCont: {
    flexDirection: "row",
    justifyContent: "space-between",
    marginVertical: 12,
  },
  msg: { fontWeight: "600" },
  request: { color: "grey" },
});
