import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  mainCont: {
    flex: 1,
    backgroundColor: "white",
  },
  sectionList: {
    flexDirection: "row",
    flexWrap: "wrap",
  },
  renderItem: {
    marginBottom: 1.5,
  },
});
